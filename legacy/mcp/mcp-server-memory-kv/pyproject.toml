[project]
name = "mcp-server-memory-kv"
version = "0.1.0"
description = "MCP memory key-value server backed by a local JSON file"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.4.1",
]
