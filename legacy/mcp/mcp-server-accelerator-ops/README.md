# mcp-server-accelerator-ops

GPU/NPU accelerator diagnostics MCP server.

It abstracts NVIDIA and Ascend providers behind the same tools. If no local accelerator CLI is available, it returns bundled mock data so demos remain reproducible on ordinary laptops.
